# Revision 3 — approved B visual contract (before implementation)

Source of truth: user-supplied [approved B moodboard](approved-b-moodboard.png), plus the latest [PR #12 Revision 3 instruction](https://github.com/Leo-P90/petid2/pull/12#issuecomment-5679332695). The moodboard is a composite concept (three ~386px-wide panels), not a literal 360dp Android capture; the native layout must retain product truth, accessibility, and functionality while reproducing its hierarchy. Baseline images are [Revision 2 QA](../revision2/README.md) and the [cache-cleared cold-start](before-cold-start.png). This contract was written before editing application code.

## Running baseline

Branch `codex/v1-slice-6-ui-b`; local HEAD = origin branch = PR #12 head `b305896aa6c785e1071c4603729d906e124ad242`. Worktree clean, no diff. Emulator `emulator-5554`; installed `com.petid.app.dev`, `versionName=0.3.0`, `versionCode=3`, updated 2026-09-15 11:09:49. `com.petid.app.preview` remains installed. Metro restarted with `--clear`; log said “Bundler cache is empty, rebuilding”. Cold-start via force-stop and dev-client deep link loaded Revision 2 Home, verified by the approved exact slogan and one global bottom bar. No Tools/FAB, “Baştan göster”, or “Demo keşfini sıfırla” appeared. A native development client does not embed a Git SHA; the defensible running-source proof is cache-cleared Metro started from the checked HEAD plus ADB UI signatures.

## Shared system

| Contract | Native acceptance |
| --- | --- |
| Palette | warm cream `#FFF9F2`, navy `#182033`, lavender `#8B7CF6`, success `#16A36A`, charcoal Match `#101116`, true filled-heart red `#F43F5E`. Surfaces white/lavender-tinted, thin warm border `#EAE4DF`, dark counterpart with controlled color accents. |
| Typography | Strong rounded display and 14–16sp readable body. Bundle Nunito (rounded sans) for display and Caveat only for one or two short ornamental phrases per screen. Both SIL OFL 1.1, official sources: https://github.com/google/fonts/tree/main/ofl/nunito and https://github.com/google/fonts/tree/main/ofl/caveat. System fallback until loaded. Record copied OFL with assets. Do not script factual labels/actions. |
| Wordmarks | Reusable PetID: “Pet” navy, “ID” lavender native colored-text/gradient equivalent plus small local paw vector; minimum 30sp on primary light screens, no plain 20sp header. Match: “Pati” white, “Match” lavender, 32–36sp. |
| Spacing | Only 4/8/12/16/24/32dp increments for structural gaps/padding. Main cards radius 20–24dp, small cards/chips 16–18dp; restrained warm edge and soft shadow. Buttons min 48dp target. |
| Illustration | Local vector/code-native cat, dog, paw and small hearts; thin strokes, no emoji. Moodboard skyline/footer is decorative and not mandatory on a 360x800 mobile screen because it would displace functional content; state this as a conscious difference in final QA. |
| Navigation | Exactly one fixed global bar with light elevated surface, icon + lavender active text and a 2–3dp small indicator; remove large purple active capsule. Match Keşfet/Mesajlar is local only. Hide bar under keyboard, preserve back/safe-area history. |
| Screens | Normal QA viewport 360x800dp; small viewport 320dp wide with comparable height. Use emulator `wm size/density` overrides and restore them. Capture screenshots with status/navigation bars visible, no dev overlays or error layers. |

## Screen composition and Revision 2 gap analysis

| Screen | Approved moodboard target | Revision 2 screenshot discrepancy | Native acceptance |
| --- | --- | --- | --- |
| PatiMatch candidate | Big two-tone wordmark + short script subtitle, refined slider icon, low wide segment with unread dot; face-led portrait ~72–78% usable space; two quiet script accents; dark gradient; 28–32sp name/age and tiny truthful approval marker; one compact breed/sex/distance row; X/info/heart integrated at photo lower edge with red glow; secondary undo, small dots. | [R2](../revision2/rev2-match.png) has tiny paw-only header, emoji gear, two tall chip tabs, Ada cat portrait with no script accents, four actions detached in a separate row and large capsule active tab. Photo focus and metadata are already sound. | Header ≤110dp, segment ≤44dp; photo and overlaid controls visually dominate, keep Ada `contentPosition` data and swipe/undo. Verification badge may be blue only when server says verified; otherwise tiny neutral marker. Candidate remains Ada/Luna fixture rather than moodboard Milo: product truth. |
| PatiMatch empty | Wordmark stays, paired cat/dog line illustration, short exact phrase and one lavender rediscover action. | [R2](../revision2/rev2-empty.png) already has one CTA but only one large cat and paw-only header; large dark void. | Center paired vector animals in soft lavender aura; no legacy debug controls in default UI. |
| Messages/Gboard | Same Match header/segment with red unread dot; thread cards and chat inputs clear over docked Gboard. | [R2 messages](../revision2/rev2-messages.png) has usable thread behavior but no wordmark, low segment or unread dot. [R2 Gboard](../revision2/rev2-gboard.png) stays clear. | Preserve two-account/live and demo messaging, unread/read semantics; keyboard screenshots show input and send above IME, no product tab duplication. |
| Health | PetID wordmark, short warm subline, bell and circular pet portrait; one tinted Milo-style pet band with name/age/breed/sex and ≤3 personality chips; big Health heading plus small script; 2x2 Aşılar/Parazit Koruması/Genel Kontrol/Diş ve Ağız, filled category icons, green success only for truthful status, concise state/last date; single lavender Sıradaki Bakım card and dog line footer. | [R2](../revision2/rev2-health.png) starts with plain “Sağlık”, two selectors, four repeated “Henüz kayıt yok”, Kilo instead of Diş, white outlined next card; sparse lower half. | Never fabricate actual user records or due dates. Demo samples clearly labelled “Örnek” may illustrate completed moodboard in an explicit fixture; real empty state uses one cohesive CTA and truthful tile statuses. Preserve weight/record/file CRUD in detail route. |
| Services 320 | PetID top + real location selector/search; strong title and tiny script; compact filled horizontal chips all reachable; two tall, photo-dominant cards per row; lavender/white category chip on photo, balanced outline favorite; business/score/count/distance/two-line warmth; secondary community links after grid. | [R2](../revision2/rev2-services-320.png) has distinct correct scenes and two columns, but plain title, huge outlined categories and favorite disks, no location/search/script, card text more visually dominant than image. | Preserve four existing category-specific local photos and crop/exposure harmony; 320dp no clipped names/actions, ≥48dp favorite, swipe reaches full Eğitmen. No made-up İstanbul: location choice sourced from user/device or clearly marked demo. |
| Home | PetID wordmark/slogan once; selected-pet summary with small species avatar and Fotoğraf ekle if empty; one care summary and exactly three quick actions, warm brand strokes with dense composition. | [R2](../revision2/rev2-home.png) has plain small PetID, two selector chips plus duplicated pet row, very large white void below quick actions, blocky cards and dev lavender active capsule. | Replace redundant selector presentation with one selected-pet band while retaining pet switching; keep profile, match and adoption paths. Illustration small and supportive. |

## Visual QA gate

At completion capture: Home light 360x800, Health light 360x800, Services light 320dp (top and lower row), Match dark candidate 360x800, Match exhausted 360x800, Messages plus docked Gboard 360x800. For each mandatory screen write a final table with `uygulandı / bilinçli fark / açık hata` against the applicable rows above; list explicit reasons for any departure. Inspect screenshots at original resolution, not just test assertions. Any open visual error blocks “moodboard matched” language and must be fixed or reported candidly.

Behavior gates: 94 existing tests plus meaningful wordmark render, dev-controls-hidden, empty, photo focus, one-bottom-bar, 320dp grid checks; TypeScript, lint, Expo Doctor, Android/web exports, native APK and update install; quality/security CI; local/origin/PR head SHA equality. Draft PR stays stacked, no merge/EAS/production.

## Revision 3 extension: approved Home pet-first reference (added before Home extension code)

The user separately supplied local `C:\Users\kaanb\Downloads\5859511160976117504.jpg` and explicitly prohibited adding it to this repository. It was inspected at original resolution in this task; this file remains local only. Instruction source: [PR #12 Home + Acil Pati addendum](https://github.com/Leo-P90/petid2/pull/12#issuecomment-5683056485). The JPG is a visual target, while the PR addendum supplies the interaction and safety constraints. In particular, the screenshot's extra four thick black navigation icons and long DEMO-card paragraphs are *not* requirements: use the PR's one native global bar and a small truthful sample label instead.

| Element | Approved Home target / direct comparison with first Revision 3 native 360x800 screenshot | Acceptance decision |
| --- | --- | --- |
| Hierarchy | Reference reads wordmark → Atlas hero → triangular Dostum/Tanış/Yuva ol → adoption preview. First native Home capture (local `rev3-home-360.png`) reads wordmark → Mia band → large generic care card → three actions; pet loses the primary path hierarchy and emergency access is absent. | Wordmark → hero → one compact three-card row → Acil Pati → adoption carousel. Remove big care card from entry; Health stays a global tab. |
| Header | Both use navy/lavender PetID and small paw, reference has small right avatar and one exact slogan. | Keep reusable wordmark; avatar alone at right; exact slogan once. |
| Hero | Reference premium lavender horizontal band with much larger pet subject, name/age and two tags, small Caveat at right. First native band uses SVG species head, text and two sample tags, so it is too flat without a supplied pet photo. | Genuine pet photo if available; otherwise local species vector in a taller left area and in-card small Fotoğraf ekle action. Card primary press opens `/profile`; compact switch sheet separate from card navigation. Include species/age and only known metadata. Do not invent breed/sex for a live pet. |
| Paths | Reference triangular arrangement is expressive but wide gaps consume vertical room. Current native three cards are in one equal row but appear below oversized care card. | Keep equal row, 80–92dp tall, ≥48dp targets, three distinct accessible destinations: `/profile`, `/match`, `/adoption`. |
| Acil Pati | Missing in reference image and first native Home. PR addendum explicitly inserts it between paths and adoption. | Warm pale coral/powder card; heading + one short prompt; coral large report CTA to `/reports`, secondary navy/lavender veterinarian CTA to `/emergency`. No auto-publish or emergency-number impression. Location requested contextually in destination flow only, with manual entry and denial copy. Real open-clinic status is unavailable, so show search/verification limitation honestly. |
| Adoption | Reference shows 2 horizontally clipped preview cards with pet illustrations and long DEMO warnings. Current Home has no carousel. | Horizontal 2–3 *local photo* demo cards with name/species-age/city/one-line warmth, model has sample marker. Small `Örnek ilanlar` on heading; click opens existing `/adoption/[id]`, `Tümünü gör` and `Yuva ol` open `/adoption`. No live-animal or application claim. |
| Navigation | Reference image ends with four black frame icons; current native has one white accessible global tab bar. | Keep native bar, no duplicate footer. |
| Viewports | Reference cropped phone dimensions differ from Android 360x800. Current native screenshot shows three actions near lower viewport; adding Acil demands denser structure. | Capture top 360x800 with primary Acil CTA at first-scroll edge if practical, full Acil + carousel by controlled scroll, and 320dp no horizontal overflow. Preserve vertical scroll and Gboard/safe area. |

Behavior contract before code: report form must allow `Başka hayvan`, retain contextual device location and manual coordinate fallback, and never publish (the current safe demo draft stays only in session). Add a photo preview and status to draft before save as the PR requests, without changing backend schema/RLS. Emergency route can assist nearby veterinary search but must not assert clinic is currently open without verified live data. No location permission on Home mount. No new service role, migration, EAS, or production action. Native QA must include pet switch, 3 routes, both Acil routes, denied location/manual fallback, no accidental publication, one tab bar, 360 and 320 captures. Conscious native difference: the exact Home JPG's pet portrait cannot be copied into the app under the user's local-reference-only rule; app shows actual selected pet photo or a code-native vector species illustration.
